# Backend runtime dependencies.
# Install with:  python -m pip install -r requirements.txt
#
# Lower bounds rather than exact pins: the previous exact pins (numpy==1.24.3)
# have no wheels for current Python versions and failed to install at all.

flask>=3.0,<4
flask-cors>=5.0,<7
python-dotenv>=1.0,<2
numpy>=1.26,<3

# Only needed to replay the demo dataset in DEMO_Model_Creation/.
requests>=2.31,<3
