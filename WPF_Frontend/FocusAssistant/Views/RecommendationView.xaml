<UserControl x:Class="FocusAssistant.Views.RecommendationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT -->
        <StackPanel Grid.Column="0" Margin="10">
            <TextBlock Text="AI Suggestions" FontWeight="Bold"/>
            <ItemsControl ItemsSource="{Binding AISuggestions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Margin="0,2"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="Most Active Hours" FontWeight="Bold" Margin="0,10,0,0"/>
            <ItemsControl ItemsSource="{Binding MostActiveHours}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Margin="0,2"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="Productivity Trend" FontWeight="Bold" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding ProductivityTrend}"/>

            <TextBlock Text="Energy Patterns" FontWeight="Bold" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding EnergyPatterns}"/>

            <TextBlock Text="Optimal Times" FontWeight="Bold" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding OptimalTimes}"/>
        </StackPanel>

        <!-- RIGHT -->
        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="Action Effectiveness" FontWeight="Bold"/>
            <ItemsControl ItemsSource="{Binding ActionEffectiveness}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="{Binding Key}" Width="200"/>
                            <TextBlock Text="{Binding Value.AverageReward,StringFormat=F2}"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="Recent Intervention" FontWeight="Bold" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding RecentInterventionMessage}" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding RecentInterventionDetails}" Margin="0,5,0,0" FontStyle="Italic"/>

            <TextBlock Text="Q-Value Stats (min / mean / max)" FontWeight="Bold" Margin="0,10,0,0"/>
            <TextBlock>
                <Run Text="{Binding QValueStats[0],StringFormat=F3}"/><Run Text=" / "/>
                <Run Text="{Binding QValueStats[1],StringFormat=F3}"/><Run Text=" / "/>
                <Run Text="{Binding QValueStats[2],StringFormat=F3}"/>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>