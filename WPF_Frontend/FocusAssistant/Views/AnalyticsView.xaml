<UserControl x:Class="FocusAssistant.Views.AnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="3" BlurRadius="6"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F3F4F6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- TODAY METRICS -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="10" Width="150">
                    <TextBlock Text="Today's Productivity" FontWeight="Bold" Foreground="#333"/>
                    <TextBlock x:Name="TodayProductivityText" FontSize="24" Foreground="#10B981"/>
                </StackPanel>
                <StackPanel Margin="10" Width="150">
                    <TextBlock Text="Today's Activities" FontWeight="Bold" Foreground="#333"/>
                    <TextBlock x:Name="TodayActivitiesText" FontSize="24" Foreground="#10B981"/>
                </StackPanel>
                <StackPanel Margin="10" Width="150">
                    <TextBlock Text="Today's Interventions" FontWeight="Bold" Foreground="#333"/>
                    <TextBlock x:Name="TodayInterventionsText" FontSize="24" Foreground="#10B981"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- ML READINESS -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Text="ML Data Readiness" FontWeight="Bold" Foreground="#333" Margin="0,0,0,10"/>
                <ProgressBar x:Name="DataReadinessProgress" Height="20" Margin="0,0,0,10" Foreground="#10B981"/>
                <TextBlock x:Name="ReadinessStatusText" FontWeight="Bold" Foreground="#333"/>
                <TextBlock x:Name="ReadinessMessageText" Foreground="#777"/>
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBlock Text="Data Points:" Foreground="#333"/>
                    <TextBlock x:Name="DataPointsText" Foreground="#333" Margin="5,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="States Explored:" Foreground="#333"/>
                    <TextBlock x:Name="StatesExploredText" Foreground="#333" Margin="5,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Feedback:" Foreground="#333"/>
                    <TextBlock x:Name="FeedbackText" Foreground="#333" Margin="5,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- CHARTS (NO EXTERNAL LIB) -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="10">
                <StackPanel>
                    <TextBlock Text="Intervention Effectiveness" FontWeight="Bold" Foreground="#333" Margin="0,0,0,10"/>
                    <ItemsControl x:Name="InterventionList"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="10">
                <StackPanel>
                    <TextBlock Text="App Usage" FontWeight="Bold" Foreground="#333" Margin="0,0,0,10"/>
                    <ItemsControl x:Name="AppUsageList"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Margin="10">
                <StackPanel>
                    <TextBlock Text="Productivity Trend" FontWeight="Bold" Foreground="#333" Margin="0,0,0,10"/>
                    <TextBlock Text="62 %  63 %  62 %  64 %  63 %  62 %  62 %" FontSize="18" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- EXPORT -->
        <Border Grid.Row="3" Style="{StaticResource CardStyle}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <ComboBox x:Name="ExportTypeCombo" Width="150" Margin="10,0,0,0">
                    <ComboBoxItem Content="All"/>
                    <ComboBoxItem Content="Sessions"/>
                    <ComboBoxItem Content="Activities"/>
                </ComboBox>
                <Button Content="Export CSV" Click="ExportCSV" Margin="10,0,0,0" Padding="10,5"/>
                <Button Content="Export JSON" Click="ExportJSON" Margin="10,0,0,0" Padding="10,5"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>