<UserControl x:Class="FocusAssistant.Views.TrackingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FocusAssistant.Converters"
             mc:Ignorable="d">
    <UserControl.Resources>
        <local:TrackingColorConverter x:Key="TrackingColorConverter"/>
        <local:TrackingButtonContentConverter x:Key="TrackingButtonContentConverter"/>
        <local:IdleStatusColorConverter x:Key="IdleStatusColorConverter"/>
        <local:ZeroCountToVisibilityConverter x:Key="ZeroCountToVisibilityConverter"/>
        <local:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:StringToDoubleConverter x:Key="StringToDoubleConverter"/>
    </UserControl.Resources>
    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding StatusText}" FontSize="16" Margin="10"/>
            <Button Content="{Binding IsTracking, Converter={StaticResource TrackingButtonContentConverter}}"
                    Click="StartTracking_Click" Margin="10"/>
            <ProgressBar Value="{Binding ProductivityScore, Converter={StaticResource StringToDoubleConverter}}"
                         Maximum="100" Height="20" Margin="10"/>
            <TextBlock Text="{Binding ProductiveTime}" FontSize="14" Margin="10"/>
            <TextBlock Text="{Binding DistractedTime}" FontSize="14" Margin="10"/>
            <TextBlock Text="{Binding ProductivityStreak}" FontSize="14" Margin="10"/>
            <ListBox ItemsSource="{Binding ActivityLog}" Margin="10">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding AppName}" Margin="5"/>
                            <TextBlock Text="{Binding DurationText}" Margin="5"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>