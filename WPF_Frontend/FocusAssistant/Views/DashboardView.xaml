<UserControl x:Class="FocusAssistant.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:FocusAssistant.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:ZeroCountToVisibilityConverter x:Key="ZeroCountToVisibilityConverter"/>

    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Dashboard Metrics -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <StackPanel Margin="10">
                <TextBlock Text="Productivity Rate" FontWeight="Bold"/>
                <TextBlock Text="{Binding ProductivityRate, FallbackValue ='Model training'}" FontSize="24"/>
            </StackPanel>
            <StackPanel Margin="10">
                <TextBlock Text="Total Activities" FontWeight="Bold"/>
                <TextBlock Text="{Binding TotalActivities, FallbackValue='Model training'}" FontSize="24"/>
            </StackPanel>
            <StackPanel Margin="10">
                <TextBlock Text="Recent Interventions" FontWeight="Bold"/>
                <TextBlock Text="{Binding RecentInterventions, FallbackValue='Model training'}" FontSize="24"/>
            </StackPanel>
        </StackPanel>

        <!-- Top Apps -->
        <StackPanel Grid.Row="1" Margin="10">
            <TextBlock Text="Top Apps" FontWeight="Bold" FontSize="18"/>
            <ItemsControl ItemsSource="{Binding TopApps}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Key}" Width="200"/>
                            <TextBlock Text="{Binding Value}"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Display 'Model training' when TopAppsCount is 0 -->
            <TextBlock Text="Model training" 
                       Visibility="{Binding TopAppsCount, Converter={StaticResource ZeroCountToVisibilityConverter}}"
                       FontStyle="Italic"/>
        </StackPanel>
    </Grid>
</UserControl>
