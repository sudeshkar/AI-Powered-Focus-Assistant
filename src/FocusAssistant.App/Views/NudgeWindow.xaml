<Window x:Class="FocusAssistant.Views.NudgeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Focus Assistant"
        Width="360" SizeToContent="Height"
        WindowStyle="None" ResizeMode="NoResize"
        ShowInTaskbar="False" Topmost="True"
        WindowStartupLocation="Manual"
        AllowsTransparency="True" Background="Transparent"
        UseLayoutRounding="True">

    <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
            BorderThickness="1" CornerRadius="10" Padding="18">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="6" Direction="270" Color="Black" Opacity="0.25"/>
        </Border.Effect>

        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <ui:SymbolIcon Symbol="Lightbulb24" FontSize="16" VerticalAlignment="Center"/>
                <TextBlock Text="Focus check-in" FontSize="13" FontWeight="SemiBold"
                           Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock x:Name="MessageText" TextWrapping="Wrap" FontSize="14" LineHeight="19"/>

            <StackPanel Orientation="Horizontal" Margin="0,16,0,0" HorizontalAlignment="Right">
                <ui:Button x:Name="ThisIsWorkButton" Content="This is work" Appearance="Transparent"
                           FontSize="12" Padding="10,6" Click="ThisIsWorkButton_Click"/>
                <ui:Button x:Name="SnoozeButton" Content="5 more minutes" Appearance="Secondary"
                           FontSize="12" Padding="10,6" Margin="8,0,0,0" Click="SnoozeButton_Click"/>
                <ui:Button x:Name="BackButton" Appearance="Primary"
                           FontSize="12" Padding="10,6" Margin="8,0,0,0" Click="BackButton_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
