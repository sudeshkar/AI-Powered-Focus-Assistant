<Page x:Class="FocusAssistant.Views.RecommendationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
        <StackPanel MaxWidth="640" HorizontalAlignment="Left">
            <TextBlock Text="Recommendations" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,16"/>

            <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="20" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Today" FontWeight="SemiBold" FontSize="14" Opacity="0.7" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding Headline}" FontSize="18" TextWrapping="Wrap"/>
                    <TextBlock Margin="0,8,0,0" Opacity="0.8">
                        <Run Text="Top apps: "/>
                        <Run Text="{Binding TopAppsSummary}"/>
                    </TextBlock>
                </StackPanel>
            </Border>

            <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="20" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Insight" FontWeight="SemiBold" FontSize="14" Opacity="0.7" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding InsightBody}" TextWrapping="Wrap" Opacity="0.85"/>
                </StackPanel>
            </Border>

            <Button Content="Refresh" Command="{Binding RefreshCommand}"
                    HorizontalAlignment="Left" Padding="16,8"/>
        </StackPanel>
    </ScrollViewer>
</Page>
