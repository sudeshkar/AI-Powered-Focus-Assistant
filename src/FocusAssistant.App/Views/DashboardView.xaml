<UserControl x:Class="FocusAssistant.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.Resources>
        <Style x:Key="MetricCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorDefaultBrush}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,16,0"/>
        </Style>
        <Style x:Key="MetricLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Opacity" Value="0.7"/>
        </Style>
        <Style x:Key="MetricValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>
    </UserControl.Resources>

    <StackPanel Margin="8">
        <TextBlock Text="Dashboard" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,16"/>

        <UniformGrid Rows="1" HorizontalAlignment="Left">
            <Border Style="{StaticResource MetricCard}" MinWidth="180">
                <StackPanel>
                    <TextBlock Text="Productivity today" Style="{StaticResource MetricLabel}"/>
                    <TextBlock Text="{Binding ProductivityRate}" Style="{StaticResource MetricValue}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource MetricCard}" MinWidth="180">
                <StackPanel>
                    <TextBlock Text="Activities tracked" Style="{StaticResource MetricLabel}"/>
                    <TextBlock Text="{Binding TotalActivities}" Style="{StaticResource MetricValue}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource MetricCard}" MinWidth="180">
                <StackPanel>
                    <TextBlock Text="Interventions today" Style="{StaticResource MetricLabel}"/>
                    <TextBlock Text="{Binding RecentInterventions}" Style="{StaticResource MetricValue}"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Border Style="{StaticResource MetricCard}" Margin="0,20,0,0" HorizontalAlignment="Stretch">
            <StackPanel>
                <TextBlock Text="Top apps today" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,12"/>
                <ItemsControl ItemsSource="{Binding TopApps}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0,4">
                                <TextBlock Text="{Binding Key}" Width="220"/>
                                <TextBlock Text="{Binding Value}" Opacity="0.7"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Text="No apps tracked yet today."
                           Opacity="0.6" FontStyle="Italic" Margin="0,4,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding TopAppsCount}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
