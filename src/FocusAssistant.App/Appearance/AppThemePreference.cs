namespace FocusAssistant.Appearance
{
    /// <summary>What the user asked for, as opposed to what is currently rendered.</summary>
    public enum AppThemePreference
    {
        /// <summary>Follows the Windows setting live, including future changes.</summary>
        System,
        Light,
        Dark,
    }
}
